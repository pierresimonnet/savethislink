<nav class="navbar">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#js-nav" aria-controls="js-nav" aria-expanded="false" aria-label="Toggle navigation" id="js-toggleBtn">
        <span class="navbar-toggler-icon">&#129386;</span>
    </button>
    <a class="navbar-brand" href={{ path("app_home") }}>Shareintel</a>
    <div class="navbar-nav-wrapper" id="js-nav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href={{ path("theme_index") }}>Explore themes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href={{ path("website_index") }}>Explore websites</a>
            </li>
        </ul>
    </div>
    {% if app.user %}
        <a class="nav-item" href={{ path("user_profile", {username: app.user.username}) }}>{{ app.user.username }}</a>
    {% else %}
        <a class="nav-item" href={{ path("app_login") }}>login</a>
    {% endif %}
</nav>