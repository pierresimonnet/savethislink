<nav class="navbar">
    <div class="navbar-toggler">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#js-nav" aria-controls="js-nav" aria-expanded="false" aria-label="Toggle navigation" id="js-toggleBtn">
            <span class="navbar-toggler-icon">{{ icon('menu') }}</span>
        </button>
    </div>
    <div class="navbar-brand">
        <a href={{ path("app_home") }}>Shareintel</a>
    </div>
    <div class="navbar-nav-wrapper" id="js-nav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path("topic_index") }}" {{ isActive('explore') }}>Explorer les sujets</a>
            </li>
        </ul>
    </div>
    <div class="navbar-auth d-flex flex-centered mr-05">
        {% if is_granted('IS_IMPERSONATOR') %}
            <a href="{{ impersonation_exit_path(path('app_home') ) }}" class="mr-05">Exit impersonation</a>
        {% endif %}
        <theme-switcher class="mr-05"></theme-switcher>
        {% if app.user %}
            <a class="d-flex flex-centered" href="{{ path("user_profile", {username: app.user.username}) }}" {{ isActive('profile') }}>{{ icon('account') }}<span class="hidden-on-mobile">{{ app.user.username }}</span></a>
        {% else %}
            <a class="d-flex flex-centered" href="{{ path("app_login") }}">{{ icon('login') }}<span class="hidden-on-mobile">Se connecter</span></a>
        {% endif %}
    </div>
</nav>