<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Shareintel{% endblock %}</title>
        <meta name="description" content="Créez vos sujets et listez simplement les liens vers les ressources web d'intérets. Ouvrez vos sujets aux contributions des utilisateurs. Découvrez des nouvelles ressources.">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            <script>
                window.USER = {{ app.user ? app.user.id : 0 }}
            </script>
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% include "partials/nav.html.twig" %}

        <main class="container-margin mt-2">
            {% include "partials/alert.html.twig" %}
            {% block body %}{% endblock %}
        </main>

        <footer class="text-center">
            <a href="{{ path('app_privacy') }}" class="decorated">Confidentialité et vie privée</a>
        </footer>
    </body>
</html>
