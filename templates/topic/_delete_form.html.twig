<form method="post" action="{{ path('topic_delete', {'id': theme.id}) }}" onsubmit="return confirm('Supprimer ce sujet ?');" class="form-delete">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
    <button class="button-secondary">{{ icon('trash') }}<span>Supprimer</span></button>
</form>
